import {StyleSheet} from 'react-native';

const RegisterUserCardStyle = StyleSheet.create({
  shadowCard: {
    borderRadius: 14,
    paddingHorizontal: 15,
    paddingVertical: 20,
    shadowColor: 'black',
    shadowOffset: {width: 0, height: 2},
    shadowOpacity: 0.14,
    shadowRadius: 24,
    backgroundColor: 'white',
  },
});

export default RegisterUserCardStyle;
