import {StyleSheet} from 'react-native';

const LockScreenBallStyle = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
    marginBottom: 50,
  },
  view: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: 134,
    height: 13,
    alignItems: 'center',
  },
});

export default LockScreenBallStyle;
